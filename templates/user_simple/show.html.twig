{% extends 'base.html.twig' %}

{% block title %}UserSimple{% endblock %}

{% block body %}
    <h1>UserSimple</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_simple.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user_simple.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user_simple.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_simple_index') }}">back to list</a>

    <a href="{{ path('app_user_simple_edit', {'id': user_simple.id}) }}">edit</a>

    {{ include('user_simple/_delete_form.html.twig') }}
{% endblock %}
