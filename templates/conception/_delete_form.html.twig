<form method="post" action="{{ path('app_conception_delete', {'id': conception.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conception.id) }}">
    <button class="btn">Delete</button>
</form>
