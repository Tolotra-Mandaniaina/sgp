<form method="post" action="{{ path('app_auto_suivi_delete', {'id': auto_suivi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auto_suivi.id) }}">
    <button class="btn btn-delete">Delete</button>
</form>
